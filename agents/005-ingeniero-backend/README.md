# 005-ingeniero-backend — puntero, no una copia

Esta carpeta NO contiene una definición independiente de este agente. Existe
solo para que `agents/` liste físicamente el 001 al 010 del Escuadrón Élite
por convención de nomenclatura (mandato del usuario, 2026-08-16).

**La definición real y única de este agente vive en:**
`.claude/agents/005-ingeniero-backend.md`

No dupliques ni edites contenido aquí — cualquier cambio de mandato, tools o
comportamiento del agente se hace en el archivo real de arriba.
